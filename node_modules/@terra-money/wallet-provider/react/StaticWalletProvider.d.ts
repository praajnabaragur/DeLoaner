import { Connection, ConnectType, WalletInfo, WalletStatus } from '@terra-dev/use-wallet';
import { NetworkInfo } from '@terra-dev/wallet-types';
import { ReactNode } from 'react';
export interface StaticWalletProviderProps {
    children: ReactNode;
    defaultNetwork: NetworkInfo;
    status?: WalletStatus;
    availableConnectTypes?: ConnectType[];
    availableInstallTypes?: ConnectType[];
    availableConnections?: Connection[];
    wallets?: WalletInfo[];
}
export declare function StaticWalletProvider({ children, defaultNetwork, status, availableConnectTypes, availableInstallTypes, availableConnections, wallets, }: StaticWalletProviderProps): JSX.Element;
